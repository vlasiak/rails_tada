%li.list-item{id: "item-#{item.id}", :'move-url' => "#{move_to_do_item_path(id: item.id)}"}
  %span.glyphicon.glyphicon-sort
  = check_box_tag "item-cb-#{item.id}", item.list_id, item.done, url: to_do_item_path(id: item.id), autocomplete: 'off'
  = label_tag "item-cb-#{item.id}", item.text