var options = {
    <% if @list.persisted? %>
        html: "<%=j render partial: 'list', locals: {list: @list_presenter} %>",
        id: <%= @list_presenter.id %>,
    <% else %>
        error: true
    <% end %>
};

var todoList = new TodoList();
todoList.onCreate(options);