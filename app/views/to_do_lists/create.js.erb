var options = {
    <% if @list.persisted? %>
        html: "<%=j render partial: 'article', locals: {list: @list} %>",
        id: <%= @list.id %>,
    <% else %>
        html: 'error'
    <% end %>
};

var todoList = new TodoList();
todoList.onCreate(options);