var options = {
    <% if @list.persisted? %>
        html: "<%=j render partial: 'list', locals: {list: @list} %>",
        id: <%= @list.id %>,
    <% else %>
        error: true
    <% end %>
};

var todoList = new TodoList();
todoList.onCreate(options);