<h1> <%= t 'list.all' %></h1>
<%= link_to t('list.new.create_button'), new_to_do_list_path,
    id: 'add-list-button', class: 'btn btn-warning' %>
<hr/>

<% @lists.each do |list| %>
    <%= render partial: 'article', locals: {list: list} %>
<% end %>